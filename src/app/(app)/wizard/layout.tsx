// DEPRECATED: Wizard layout is no longer used
// All wizard routes now redirect to new project-centric routes
export default function WizardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
