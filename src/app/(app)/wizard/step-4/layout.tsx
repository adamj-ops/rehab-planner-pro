// DEPRECATED: Step-4 layout is no longer used
// All step-4 routes now redirect to new project-centric routes
export default function Step4Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
